# Portfolio Frontend (Next.js + Tailwind + Framer Motion)
# milon-dev


Make sure the backend (`../server`) is running first — the landing page's Skills/Projects
sections and the Blog/Admin pages all call it live (with sensible fallback content shown
if the API isn't reachable yet).
